<form method="post" action="{{ path('app_bid_delete', {'id': bid.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bid.id) }}">
    <button class="btn">Delete</button>
</form>
